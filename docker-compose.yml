services:
  web:
    build: .
    container_name: okta-bulk-tool
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./private_jwk.json:/run/secrets/okta_private_jwk.json:ro
