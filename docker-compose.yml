services:
  web:
    build: .
    container_name: okta-bulk-tool
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - activation_data:/data

volumes:
  activation_data:
